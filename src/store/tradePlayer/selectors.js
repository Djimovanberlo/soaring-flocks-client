export const selectTradePlayer = (state) => state.tradePlayer;

export const selectTradeState = (state) => state.tradePlayer.tradeState;

export const selectTradeText = (state) => state.tradePlayer.tradeText;
