export const selectToken = (state) => state.player.token;

export const selectPlayer = (state) => state.player.player;

export const selectPlayerId = (state) => state.player.player.id;

export const selectPlayerName = (state) => state.player.player.name;
